---
---

<section
    id="productos-hero"
    class="relative min-h-[70vh] flex items-center justify-center overflow-hidden bg-linear-to-br from-red-900 to-red-700"
>
    <div class="absolute inset-0 opacity-20">
        <img
            src="/productos/productos-hero.png"
            alt="Productos CYM Industrial"
            class="w-full h-full object-cover"
        />
    </div>

    <div class="relative max-w-7xl mx-auto px-6 lg:px-8 w-full">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div class="text-white space-y-8">
                <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
                    PRODUCTOS
                </h1>
                <p class="text-xl text-gray-300 leading-relaxed">
                    Soluciones integrales en sistemas de aire comprimido. Compresores de tornillo y tratamiento de aire de la marca Chicago Pneumatic.
                </p>
            </div>

            <div class="relative hidden lg:block">
                <img
                    src="/productos/productos-hero.png"
                    alt="Compresores Industriales CYM"
                    class="w-full max-w-2xl mx-auto object-contain"
                />
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    h1 {
        animation: fadeInUp 0.8s ease-out forwards;
    }

    p {
        animation: fadeInUp 0.8s ease-out 0.2s forwards;
        opacity: 0;
    }
</style>
